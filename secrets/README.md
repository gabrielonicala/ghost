# Secrets Folder

This folder contains sensitive credential files that should **never** be committed to git.

## Files in this folder:

- `google-cloud-service-account.json` - Google Cloud service account key for Video Intelligence API

## Security

All files in this folder are automatically ignored by git (see `.gitignore`).
